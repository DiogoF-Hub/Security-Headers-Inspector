{
  "name": "security-headers-inspector",
  "version": "1.6.6",
  "private": true,
  "description": "Development tooling for the Security Headers Inspector browser extension",
  "scripts": {
    "test": "node --test tests/unit/*.test.js",
    "test:e2e": "node --test --test-concurrency=1 tests/e2e/*.test.js"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  },
  "engines": {
    "node": ">=22"
  }
}
